- name: variables
  hosts: webservers
  become: yes
  vars:
  NAME: Naresh
  village: Ramapuram
  tasks:
   - name: print the variables
     ansible.builtin.debug:
      - msg: Hello {{ NAME }} I am from {{ village }}