- name: Print the variables from file
  hosts: webservers
  vars_files:
    - vars.yaml  # This is where your variables are defined
  tasks:
    - name: Print a greeting message
      ansible.builtin.debug:
        msg: "Hello {{ NAME }}, I am from {{ village }}"
